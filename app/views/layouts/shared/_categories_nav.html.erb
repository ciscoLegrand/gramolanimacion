<% @categories.each do |category| %>
  <li class="hover:scale-125 transition-all duration-500">
    <%= link_to category.name, site_category_path(category),  class: 'text-black-600 mx-2 sm:mx-4 capitalize tracking-wide hover:border-b-4 hover:border-indigo-500  hover:text-indigo-500 transition-all duration-500' %>
  </li>
<% end %>
