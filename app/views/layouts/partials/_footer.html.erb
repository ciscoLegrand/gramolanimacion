    <footer class="border-t border-gray-200 h-40 py-4 relative mt-8">
      <div class="container flex flex-col flex-wrap  px-4 mx-auto md:items-center lg:items-start md:flex-row md:flex-nowrap">
        <div class="flex-shrink-0 w-64 mx-auto text-center md:mx-0 md:text-left" >
          <%= link_to inline_svg_tag('ciscolegrandlogo.svg', size:'80px',class: 'self-center text-lg font-semibold whitespace-nowrap dark:text-white'), root_path , class:"flex items-center justify-center text-4xl font-bold text-blue-700 md:justify-start"%>
        </div>
        <div class="justify-between w-full mt-4 text-center lg:flex">          
          <div class="w-full px-4 lg:w-1/3 md:w-1/2">
            <h2 class="mb-2 font-bold tracking-widest text-gray-900">
              Apartados legales
            </h2>
            <% LegalText.all.each do |legaltext| %>
              <%= link_to legaltext.title, site_legal_text_path(legaltext) %>
            <% end %>
          </div>
        </div>
      </div>
      <div class="flex justify-center">
        <p class="text-base text-gray-400">
          All rights reserved by creaDix <%= Time.now.year %>
        </p>
      </div>
    </footer>