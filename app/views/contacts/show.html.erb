<div class="w-full">
  <div class="bg-gradient-to-b from-blue-800 to-blue-600 h-96 w-full"></div>
  <div class="max-w-5xl mx-auto px-6 sm:px-6 lg:px-8 mb-12">
    <div class="bg-white w-full shadow rounded p-8 sm:p-12 -mt-72">
      <h1 class="font-bold text-2xl flex justify-center"> <%= "#{@contact&.name} #{@contact&.last_name}" %></h1>
      <p class="font-bold text-2xl flex justify-center"> <%= mail_to @contact.email %></p>  
      <p><%= @contact.content %></p>
    </div>
  </div>
</div>