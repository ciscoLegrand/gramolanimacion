<%= render ModalComponent.new(title: @legal_text.title) do %>
  <%= turbo_frame_tag 'legaltext' do %>
    <div class="flex flex-col w-full">
      <h1 class="font-bold text-4xl flex justify-center"> <%= @legal_text.title  %></h1>
      <p><%= @legal_text.content %></p>
    </div>
  <% end %>
<% end %>