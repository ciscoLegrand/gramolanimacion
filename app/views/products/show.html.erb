<%= render ModalComponent.new(title: @product.name) do %>
  <%= turbo_frame_tag 'product' do %>
    <div class="flex flex-col w-full">
      <h1 class="font-bold text-4xl flex justify-center"> <%= @product.name  %></h1>
      <p><%= @product.description %></p>
    </div>
  <% end %>
<% end %>